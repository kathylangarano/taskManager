//import { Navigate, Outlet, useLocation } from "react-router-dom";
//import useAuth from "../auth/useAuth";
//import routes from "../helpers/routes";



//function PrivateRoute({hasRole: role} ){
   // const location = useLocation();

   // const {hasRole, isLogged } = useAuth();

    //if(role && !hasRole(role)) return <Navigate to={routes.home}/>;
    // if(!isLogged()) return <Navigate to={routes.login}/>;
    
   // return isLogged?<Outlet/>:<Navigate to={{pathname: routes.login, state:{from:location}}}/>;
        
//}
//export default PrivateRoute